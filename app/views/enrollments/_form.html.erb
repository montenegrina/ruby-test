<%= form_with(model: enrollment, local: true) do |form| %>
  <% if enrollment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>

      <ul>
      <% enrollment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :enrollment_date %>
  </div>

  <div class="field">
    <%= form.label :method %>
    <%= form.text_field :method, id: :enrollment_method %>
  </div>

  <div class="field">
    <%= form.label :remarks %>
    <%= form.text_field :remarks, id: :enrollment_remarks %>
  </div>

  <div class="field">
    <%= form.label :coordinator_id %>
    <%= form.text_field :coordinator_id, id: :enrollment_coordinator_id %>
  </div>

  <div class="field">
    <%= form.label :registry_id %>
    <%= form.text_field :registry_id, id: :enrollment_registry_id %>
  </div>

  <div class="field">
    <%= form.label :participant_id %>
    <%= form.text_field :participant_id, id: :enrollment_participant_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
